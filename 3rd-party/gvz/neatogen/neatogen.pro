include( ../gvz-ard-common.pro )
INCLUDEPATH += $${NEATOGEN_DIR} $${COMMON_DIR} $${CGRAPH_DIR} $${GVC_DIR} $${SPARSE_DIR} $${PATHPLAN_DIR} $${ORTHO_DIR} $${SFDPGEN_DIR}
SOURCES += $${NEATOGEN_SOURCES}
include( ../print-config.pro )
