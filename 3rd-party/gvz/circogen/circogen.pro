include( ../gvz-ard-common.pro )
INCLUDEPATH += $${CIRCOGEN_DIR} $${COMMON_DIR} $${CGRAPH_DIR} $${GVC_DIR} $${NEATOGEN_DIR} $${SPARSE_DIR}
SOURCES += $${CIRCOGEN_SOURCES}
include( ../print-config.pro )

